import { Switch } from "react-router-dom";
import Home from "../Pages/Home";

const Routes = () => {
  return <Switch>
    <Routes>
      <Home />
    </Routes>
  </Switch>;
};

export default Routes;
